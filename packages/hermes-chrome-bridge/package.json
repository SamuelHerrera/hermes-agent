{
  "name": "@hermes/chrome-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local MCP server for the Hermes Chrome bridge",
  "type": "module",
  "bin": {
    "hermes-chrome-bridge": "dist/server.js",
    "hermes-chrome-bridge-install-host": "dist/native/install-host.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.native.json",
    "check": "npm run typecheck && npm run lint && npm run build && npm run test",
    "install-host": "node dist/native/install-host.js",
    "lint": "eslint src/ native/",
    "lint:fix": "eslint src/ native/ --fix",
    "prepare": "npm run build",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.native.check.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=22.22.0"
  }
}