# Hermes Omarchy layout sketches

Open `index.html` in a browser. The file contains three switchable mockups:

- A — continuous horizontal workspace
- B — desktops + focused viewport
- C — dense tiled matrix

These are disposable planning mockups only; no Desktop app source was changed.
